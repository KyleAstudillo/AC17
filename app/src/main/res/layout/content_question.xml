<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_question"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.kyle.myapplication.QuestionActivity"
    tools:showIn="@layout/activity_question">
    

    <EditText
        android:id="@+id/text_field"
        android:labelFor="@+id/text_field"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:hint="@string/text_place_holder_text"
        android:gravity="center"
        android:inputType="text"
        />

    <TextView
        android:id="@+id/question_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/text_field"
        android:text="@string/question_label_text"
        android:textSize="20sp"
        android:gravity="center"
        />

    <Button
        android:id="@+id/submit_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_field"
        android:text="@string/submit_button_text"
        android:gravity="center"
        />

    <TextView
        android:id="@+id/answer_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/submit_button"
        android:text="@string/answer_label_default"
        android:textSize="20sp"
        android:gravity="center"
        />

</RelativeLayout>
